There is intended to be an empty folder called manual-output in this directory
This folder is meant to be filled with the output from AssetRipper, it will
copy whatever is inside to the build directory so the application can access 
the files easily/simply.